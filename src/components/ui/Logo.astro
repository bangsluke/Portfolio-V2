---
import { Icon } from 'astro-icon/components';

interface Props {
	class?: string;
}

const { class: className = '' } = Astro.props;
---

<Icon
	name="logo"
	class={`${className} dark:invert dark:brightness-0 dark:contrast-200 hover:text-theme-300 hover:scale-150 transition-all`}
	aria-hidden="true"
/>
